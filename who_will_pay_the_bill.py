import random

friends = ["Alice", "Bob", "Charlie", "David", "Emanuel"]
# print(friends[random.randint(0, len(friends)-1)]) one way of doing it.
print(random.choice(friends))  # another way of doing the same thing.
